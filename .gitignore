# Python
__pycache__/
*.pyc
.venv/
venv/

# Local Claude Code tooling state (session/scheduling data, not project content)
/.claude/

# Secrets
.env

# Agent-workflow files are local-only, never committed (see global CLAUDE.md Rule 18)
/CLAUDE.md
/workflow_status.md
/workflow_status_archive.md

# Local-only input materials excluded from version control - not ours to
# redistribute publicly. Only OUR generated output (examples/*.pdf) is ever
# committed. Deliberately generic: no filenames or source identifiers appear
# here or anywhere else in a tracked file.
/local-input/
